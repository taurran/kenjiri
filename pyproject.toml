[project]
name = "kenjiri"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.0",
    "pygame-ce>=2.5.7",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
    "pytest>=9.1.1",
]
