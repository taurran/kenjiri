"""Kenjiri — a chibi-corgi block puzzle.

Package layout (D23): ``game/`` pure rules logic, ``gfx/`` sprites and
particles, ``ui/`` scenes + HUD + app shell, ``audio/`` the SFX/music
engine, ``persistence/`` high-score storage and logging. Run the game with
``python -m kenjiri`` (add ``--smoke`` for the headless self-check).
"""
from __future__ import annotations

__version__ = "0.1.0"
